server:
  port: 8080

spring:
  datasource:
    url: jdbc:mariadb://localhost:3307/dadamda?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: dadamda
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    # 최소 32바이트 이상 권장. 운영에서는 Secret Manager/환경변수로 관리.
    secret: 0123456789abcdef0123456789abcdef
    issuer: "social-rn"
    access-token-minutes: 60

  google:
    # RN Google Sign-In에서 발급받는 ID Token의 aud 검증용 (보통 Web client id)
    client-id: 349742149888-pk25o4qqsdjuisfv0kf8t4lip258u4cj.apps.googleusercontent.com

  kakao:
    userinfo-url: "https://kapi.kakao.com/v2/user/me"